diff --git a/frontend/eslint.config.js b/frontend/eslint.config.js
index 085fa79..ecf97e2 100644
--- a/frontend/eslint.config.js
+++ b/frontend/eslint.config.js
@@ -18,12 +18,17 @@ export default [
       "react-hooks": reactHooks,
       "react-refresh": reactRefresh,
     },
+    settings: {
+      react: { version: "detect" },
+    },
     rules: {
       ...js.configs.recommended.rules,
+      ...reactPlugin.configs.flat.recommended.rules,
       ...reactHooks.configs.recommended.rules,
       "react-refresh/only-export-components": ["warn", { allowConstantExport: true }],
       "react/jsx-uses-react": "off",
       "react/react-in-jsx-scope": "off",
+      "react/prop-types": "off",
     },
   },
 ];
diff --git a/frontend/src/App.jsx b/frontend/src/App.jsx
index be014cd..8f515e0 100644
--- a/frontend/src/App.jsx
+++ b/frontend/src/App.jsx
@@ -473,7 +473,7 @@ function EscrowCard({ address, publicKey, onActionComplete, demoMode, refreshKey
                   />
                   {!deadlinePassed && ledgersRemaining != null && (
                     <span className="text-[10px] text-gray-500">
-                      Deadline'a ~{ledgersRemaining} ledger var
+                      ~{ledgersRemaining} ledgers until deadline
                     </span>
                   )}
                 </div>
@@ -515,7 +515,7 @@ function ActionButton({ label, loading, disabled, onClick, className }) {
     <button
       onClick={onClick}
       disabled={loading || disabled}
-      title={disabled && !loading ? "Bu işlem şu an için kullanılamıyor" : undefined}
+      title={disabled && !loading ? "This action is not available right now" : undefined}
       className={`px-3 py-1.5 text-xs font-semibold rounded-lg text-white disabled:opacity-50 disabled:cursor-not-allowed transition-colors ${className}`}
     >
       {loading ? (
diff --git a/frontend/src/contracts.js b/frontend/src/contracts.js
index 6aaee86..6856689 100644
--- a/frontend/src/contracts.js
+++ b/frontend/src/contracts.js
@@ -56,16 +56,16 @@ function normalizeEscrowData(data) {
 // ---------------------------------------------------------------------------
 
 const CONTRACT_ERROR_MESSAGES = {
-  1: "Bu escrow hâlâ 'Pending' durumunda; bu işlem ancak fon yatırılmadan önce yapılabilir.",
-  2: "Bu işlem yalnızca 'Funded' durumundaki bir escrow üzerinde yapılabilir.",
-  3: "Bu escrow'da açık bir anlaşmazlık (dispute) yok.",
-  4: "Deadline ledger'ı henüz gelmedi; timeout talebi için erken.",
-  5: "Deadline zaten geçmiş görünüyor; bu haliyle escrow oluşturulamaz.",
-  6: "Geçersiz tutar (0 veya negatif olamaz).",
-  7: "Buyer ve seller aynı adres olamaz.",
-  8: "Bu işlemi yapmaya yetkin yok — cüzdanın bu escrow'daki buyer/seller/arbiter rolüyle eşleşmiyor.",
-  9: "Escrow bulunamadı ya da henüz başlatılmamış.",
-  10: "Kontrat WASM hash'i ayarlanmamış (factory yapılandırma sorunu).",
+  1: "This escrow is still in 'Pending' status; this action is only available before it's funded.",
+  2: "This action is only available on an escrow that is in 'Funded' status.",
+  3: "This escrow has no open dispute.",
+  4: "The deadline ledger hasn't been reached yet; it's too early for a timeout request.",
+  5: "The deadline appears to have already passed; an escrow can't be created like this.",
+  6: "Invalid amount (cannot be zero or negative).",
+  7: "The buyer and seller cannot be the same address.",
+  8: "You're not authorized to perform this action — your wallet doesn't match the buyer/seller/arbiter role on this escrow.",
+  9: "Escrow not found or not yet initialized.",
+  10: "Contract WASM hash is not set (factory configuration issue).",
 };
 
 function parseSimulationErrorCode(message) {
@@ -83,7 +83,7 @@ function describeSimulationFailure(err) {
   const friendly =
     code != null && CONTRACT_ERROR_MESSAGES[code]
       ? CONTRACT_ERROR_MESSAGES[code]
-      : "İşlem simülasyonu başarısız oldu. Sözleşme koşulları şu an sağlanmıyor olabilir.";
+      : "Transaction simulation failed. The contract's conditions may not currently be met.";
   const wrapped = new Error(friendly);
   wrapped.cause = raw;
   return wrapped;
@@ -95,9 +95,9 @@ function describeSimulationFailure(err) {
 // number, so we can only give a generic, non-cryptic explanation here.
 function describeSubmissionFailure(rawXdrOrMessage) {
   const wrapped = new Error(
-    "İşlem zincire gönderildi ama başarısız oldu. Muhtemelen bu escrow'un durumu, işlemin " +
-      "onaylanmasından hemen önce başka bir işlemle değişti (örn. karşı taraf zaten release/dispute açtı). " +
-      "Sayfayı yenileyip güncel durumu kontrol et."
+    "The transaction was submitted to the network but failed. This escrow's status likely " +
+      "changed by another transaction right before this one was confirmed (e.g. the other party " +
+      "already released/disputed it). Refresh the page and check the current status."
   );
   wrapped.cause = rawXdrOrMessage;
   return wrapped;
