[workspace]
resolver = "2"
members = [
    "contracts/common",
    "contracts/escrow",
    "contracts/factory",
]

[workspace.dependencies]
soroban-sdk = "27.0.0"

[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
lto = true
codegen-units = 1
panic = "abort"
